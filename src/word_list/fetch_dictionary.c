#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void print_fetch_options() {
	printf("This program is not compiled with libcurl so it have to depend on your system's installation of curl or PowerShell.\n");
}
